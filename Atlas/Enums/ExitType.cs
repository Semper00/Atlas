namespace Atlas.Enums
{
    /// <summary>
    /// Used for indicating the pocket dimension exit's type.
    /// </summary>
    public enum ExitType
    {
        /// <summary>
        /// Indicates that this exit is in fact an exit.
        /// </summary>
        Exit = 1,
        
        /// <summary>
        /// Indicates that this exit is a killer.
        /// </summary>
        Killer = 0
    }
}
