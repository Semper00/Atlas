namespace Atlas.Enums
{
    /// <summary>
    /// Represents the two in-game player report types.
    /// </summary>
    public enum ReportType
    {
        /// <summary>
        /// Represents a report for the server administrators.
        /// </summary>
        Local,

        /// <summary>
        /// Represents a report for the global moderation team.
        /// </summary>
        Global
    }
}
